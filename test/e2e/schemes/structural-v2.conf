# AIDA Evaluation Scheme: Structural Capability v2
# Full D1-D11 engine tests + IdleX GEO business scenario (8 Agent turns)
# Duration: ~15 min (full) / ~3 min (engine-only)

name=structural-v2
description="D1-D11 Structural + IdleX GEO Business Scenario"

# Which test script to delegate to
script=structural-capability.sh
log_dir=/tmp/structural-capability

# Scoring rubric
rubric=structural.json

# Model configuration
model_env=SC_MODEL
model_default=dashscope/qwen3.5-plus

# Supported flags (forwarded to underlying script)
# --skip-install --engine-only --phase N
