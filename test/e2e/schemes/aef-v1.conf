# AIDA Evaluation Scheme: AEF Capability v1
# Σ1-Σ11 eleven-dimension evaluation + IdleX GEO business scenario
# Duration: ~20 min (full) / ~3 min (engine-only)

name=aef-v1
description="AEF 11-Dimension (Σ1-Σ11) + IdleX GEO Business Scenario"

# Which test script to delegate to
script=aef-capability.sh
log_dir=/tmp/aef-capability

# Scoring rubric
rubric=aef.json

# Model configuration (hardcoded in aef-capability.sh)
model_env=SC_MODEL
model_default=dashscope/qwen3.5-plus
