# Mock scheme for framework testing (local, no remote server)
name=test-mock
description="Mock test for framework validation (local only)"

script=lib/mock-test.sh
log_dir=/tmp/aida-eval-mock

rubric=structural.json

model_env=SC_MODEL
model_default=mock/test-model
